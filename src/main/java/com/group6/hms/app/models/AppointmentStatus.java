package com.group6.hms.app.models;

/**
 * The {@code AppointmentStatus} enum represents the various stages of an
 * appointment within the system. It includes statuses such as requested,
 * confirmed, cancelled, and completed.
 */
public enum AppointmentStatus {
    REQUESTED,
    CONFIRMED,
    CANCELLED,
    COMPLETED
}